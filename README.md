# Python


# Fast API

FastAPI is a modern Python framework used to build APIs (backend services) quickly and efficiently.

Think of it like this:

Your frontend/app → sends a request → FastAPI → processes it → sends back data
